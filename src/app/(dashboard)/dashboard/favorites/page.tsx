export default function FavoritesPage() {
  return (
    <div>
      <h1 className="font-display text-4xl text-navy">Favorites</h1>
      <p className="mt-3 text-muted">Save finished songs to favorites from the listening page. Empty for now.</p>
    </div>
  );
}
